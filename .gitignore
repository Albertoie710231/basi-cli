.zig-cache/
zig-out/
basi-cli
*.o

# per-project vector-store sidecars (embeddings + reuse-gate symbol index)
.basi/.symbols.bin
.basi/.reuse_cand.c
.basi/knowledge/.embeddings.bin

# generated, per-project, user-editable llama-server launch script
.basi/run-llama-server.sh

# generated clangd artifact (contains absolute local paths)
compile_commands.json
# stray screenshots
Captura de pantalla_*.png

# note-cli mock test artifacts (manager/worker experiment)
noted/
pyproject.toml
*.egg-info/
tests/mem_bench/last_run.md

# per-session agent journals
.basi/journal-*.md

# factory fixtures are GENERATED by gen_fixtures.py (the generator is tracked)
scripts/factory-testfixtures/_fix/
